/**
 * App providers — optional wrapper for global providers (e.g. Helmet, Router).
 * Currently Router is in main.tsx; SmoothScrollProvider can be added here if used.
 */
export { SmoothScrollProvider } from '@/components/providers/SmoothScrollProvider';
